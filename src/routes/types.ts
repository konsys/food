export enum EPathName {
  HOME = 'HOME',
  MENU = 'MENU',
  CHECKOUT = 'CHECKOUT',
  MOBILE_CART = 'MOBILE_CART',
  NOT_FOUND = 'NOT_FOUND',
}
export type TPath = {
  path: string;
  name: string;
};
