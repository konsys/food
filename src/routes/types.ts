export enum EPath {
  HOME = "HOME",
  FOOD_MENU = "FOOD_MENU",
  ABOUT = "ABOUT",
  CONTACTS = "CONTACTS",
  LOGIN = "LOGIN",
  NOT_FOUND = "NOT_FOUND"
}
export type TMenuNames = {
  path: string;
  name: string;
};
