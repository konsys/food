export enum EPathName {
  HOME = 'HOME',
  MENU = 'MENU',
  MENU_ITEM = 'MENU_ITEM',
  ABOUT = 'ABOUT',
  CONTACTS = 'CONTACTS',
  LOGIN = 'LOGIN',
  NOT_FOUND = 'NOT_FOUND',
}
export type TPath = {
  path: string;
  name: string;
};
