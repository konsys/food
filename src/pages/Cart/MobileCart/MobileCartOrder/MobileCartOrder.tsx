import React, { memo } from 'react';

interface Props {}

function MobileCartOrder(props: Props) {
  const {} = props;

  return <span>MobileCartOrder</span>;
}

export default memo(MobileCartOrder);
